
product: jvm/app

dependencies:
  - $ktor.server.core
  - $ktor.server.netty
  - $ktor.server.auth
  - $ktor.server.contentNegotiation
  - $ktor.server.statusPages
  - io.ktor:ktor-server-routing-openapi
  - $ktor.serialization.kotlinx.json
  - $libs.logback.classic
  - $libs.exposed.core
  - $libs.exposed.jdbc
  - $libs.hikari
  - $libs.postgresql
  - $libs.kotlinx.coroutines.core
  - $libs.flyway.core
  - $libs.flyway.postgresql

test-dependencies:
  - $ktor.server.testHost
  - $libs.h2

settings:
  ktor: enabled
  kotlin:
    serialization: json
